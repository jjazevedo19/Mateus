SELECT b.* 
FROM (
	SELECT COPIAS_VENDIDAS,ID_LIVRO, ROWNUM linha 
	FROM 
	 (SELECT sum(a.COPIAS_VENDIDAS), a.ID_LIVRO
	  FROM LIVROS a, AUTORES b
	  WHERE a.ID_AUTOR = b.ID_AUTOR
	  GROUP BY a.ID_AUTOR 
	  ORDER BY 1 DESC
	 )
     )
WHERE linha <= 3 
