SELECT * FROM 
	(SELECT COPIAS_VENDIDAS,ID_LIVRO, ROWNUM linha FROM 
	 (SELECT min(a.COPIAS_VENDIDAS) COPIAS_VENDIDAS , a.ID_LIVRO 
     	  FROM LIVROS  a
	  GROUP BY a.ID_LIVRO 
	  ORDER BY 1 
	 )
	)
WHERE linha <= 3
